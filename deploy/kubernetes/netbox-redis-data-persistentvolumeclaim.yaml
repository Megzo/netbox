apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: netbox-redis-data
  name: netbox-redis-data
  namespace: netbox
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
