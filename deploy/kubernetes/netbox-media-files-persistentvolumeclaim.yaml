apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: netbox-media-files
  name: netbox-media-files
  namespace: netbox
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: netbox-media-files
  resources:
    requests:
      storage: 100Mi
