apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: netbox-nginx-config
  name: netbox-nginx-config
  namespace: netbox
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Mi

